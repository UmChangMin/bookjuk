<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookjuk.book.dao.mapper.BookMapper">

	<select id="bookCount" resultType="int">
		SELECT nvl(count(book_num),0) FROM book
	</select>
	
	<select id="bookMainCateCount" parameterType="String" resultType="int">
		SELECT nvl(count(b.book_num),0) FROM book b, category c WHERE b.book_num = c.book_num and c.category_main_eng = #{category}
	</select>
	
	<select id="bookSubCateCount" parameterType="String" resultType="int">
		SELECT nvl(count(b.book_num),0) FROM book b, category c WHERE b.book_num = c.book_num and c.category_sub_eng = #{category}
	</select>

	<select id="bookMainCateList" parameterType="java.util.Map" resultType="com.bookjuk.book.dto.BookDto">
		<![CDATA[
		SELECT j.book_num, j.book_img, j.book_name, j.book_author, j.book_publisher, j.book_date, j.book_intro, j.product_price FROM (SELECT i.*, ROWNUM rnum FROM(SELECT b.book_num, b.book_img, b.book_name, b.book_author, b.book_publisher, b.book_date, b.book_intro, p.product_price FROM book b, CATEGORY c, product p WHERE c.book_num = b.book_num AND b.book_num = p.book_num AND c.category_main_eng = #{category})i)j WHERE j.rnum >= #{startRow} AND j.rnum <= #{endRow}
		]]>
	</select>
	
	<select id="bookSubCateList" parameterType="java.util.Map" resultType="com.bookjuk.book.dto.BookDto">
		<![CDATA[
		SELECT j.book_num, j.book_img, j.book_name, j.book_author, j.book_publisher, j.book_date, j.book_intro, j.product_price FROM (SELECT i.*, ROWNUM rnum FROM(SELECT b.book_num, b.book_img, b.book_name, b.book_author, b.book_publisher, b.book_date, b.book_intro, p.product_price FROM book b, CATEGORY c, product p WHERE c.book_num = b.book_num AND b.book_num = p.book_num AND c.category_sub_eng = #{category})i)j WHERE j.rnum >= #{startRow} AND j.rnum <= #{endRow}
		]]>
	</select>

	<select id="bestList" parameterType="java.util.Map" resultType="com.bookjuk.book.dto.BookDto">
		<![CDATA[
		SELECT j.book_num, j.book_img, j.book_name, j.book_author, j.book_publisher, j.book_date, j.book_intro, j.product_price FROM (SELECT i.*, ROWNUM rnum FROM(SELECT b.book_num, b.book_img, b.book_name, b.book_author, b.book_publisher, b.book_date, b.book_intro, p.product_price FROM book b, CATEGORY c, product p WHERE c.book_num = b.book_num AND b.book_num = p.book_num ORDER BY product_sales DESC)i)j WHERE j.rnum >= #{startRow} AND j.rnum <= #{endRow}
		]]>
	</select>

	<select id="newList" parameterType="java.util.Map" resultType="com.bookjuk.book.dto.BookDto">
		<![CDATA[
		SELECT j.book_num, j.book_img, j.book_name, j.book_author, j.book_publisher, j.book_date, j.book_intro, j.product_price FROM (SELECT i.*, ROWNUM rnum FROM(SELECT b.book_num, b.book_img, b.book_name, b.book_author, b.book_publisher, b.book_date, b.book_intro, p.product_price FROM book b, CATEGORY c, product p WHERE c.book_num = b.book_num AND b.book_num = p.book_num ORDER BY book_date DESC)i)j WHERE j.rnum >= #{startRow} AND j.rnum <= #{endRow}
		]]>
	</select>
	
	<select id="issueList" parameterType="java.util.Map" resultType="com.bookjuk.book.dto.BookDto">
		<![CDATA[
		SELECT j.book_num, j.book_img, j.book_name, j.book_author, j.book_publisher, j.book_date, j.book_intro, j.product_price FROM (SELECT i.*, ROWNUM rnum FROM(SELECT b.book_num, b.book_img, b.book_name, b.book_author, b.book_publisher, b.book_date, b.book_intro, p.product_price FROM book b, CATEGORY c, product p WHERE c.book_num = b.book_num AND b.book_num = p.book_num ORDER BY product_count DESC)i)j WHERE j.rnum >= #{startRow} AND j.rnum <= #{endRow}
		]]>
	</select>
	
	<select id="discountList" parameterType="java.util.Map" resultType="com.bookjuk.book.dto.BookDto">
		<![CDATA[
		SELECT j.book_num, j.book_img, j.book_name, j.book_author, j.book_publisher, j.book_date, j.book_intro, j.product_price FROM (SELECT i.*, ROWNUM rnum FROM(SELECT b.book_num, b.book_img, b.book_name, b.book_author, b.book_publisher, b.book_date, b.book_intro, p.product_price FROM book b, CATEGORY c, product p WHERE c.book_num = b.book_num AND b.book_num = p.book_num ORDER BY book_date ASC)i)j WHERE j.rnum >= #{startRow} AND j.rnum <= #{endRow}
		]]>
	</select>
	
	<select id="reviewCount" parameterType="int" resultType="int">
		SELECT nvl(count(*),0) FROM member_review WHERE book_num = #{book_num}
	</select>

	<select id="reviewDetail" parameterType="int" resultType="com.bookjuk.book.dto.BookDto">
		SELECT A.* FROM (SELECT b.book_num, book_img, book_name, book_author, book_publisher, book_date, book_score, book_editor, book_contents, book_intro, book_author_info, book_publisher_review, category_main_kor, category_sub_kor, category_main_eng, category_sub_eng, product_amount, product_price, product_dicount, product_point, product_sales, product_count, product_delivery, review_num, review_score, member_id, review_content, review_date FROM book b, CATEGORY c, product p, member_review m WHERE c.book_num = b.book_num AND b.book_num = p.book_num AND b.book_num = m.book_num)A WHERE A.book_num = #{book_num}
	</select>

	<select id="detail" parameterType="int" resultType="com.bookjuk.book.dto.BookDto">
		SELECT a.* FROM (SELECT b.book_num, book_img, book_name, book_author, book_publisher, book_date, book_score, book_editor, book_contents, book_intro, book_author_info, book_publisher_review, category_main_kor, category_sub_kor, category_main_eng, category_sub_eng, product_amount, product_price, product_dicount, product_point, product_sales, product_count, product_delivery FROM book b, CATEGORY c, product p WHERE c.book_num = b.book_num AND b.book_num = p.book_num)a WHERE a.book_num = #{book_num}
	</select>
</mapper>
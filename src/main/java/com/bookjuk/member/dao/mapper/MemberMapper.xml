<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookjuk.member.dao.mapper.MemberMapper">

	<select id="checkId" parameterType="String" resultType="com.bookjuk.admin.dto.AdminLocationDto" >
			select * from location where location_name=#{location_name}
	</select>

</mapper>
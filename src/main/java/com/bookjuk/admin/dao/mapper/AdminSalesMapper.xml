<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookjuk.admin.dao.mapper.AdminLocationMapper">
	
	<select id="today_sales" parameterType="String" resultType="long">
		<![CDATA[
			select sum(order_total_price) FROM book_order WHERE order_date between to_date(#{to}#,'yyyyDDMM') AND TO_DATE(#{to}#,'yyyyMMDD')+0.99999
		]]>
	</select>
	
	
</mapper>